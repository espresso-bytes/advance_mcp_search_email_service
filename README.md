## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install` or `npm i`
2. Set the `GEMINI_API_KEY`,`SUPABASE_KEY` in [.env.local](.env.local)
3. Run the app:
   `npm run dev`
